name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    runs-on: ubuntu-latest

    env:
      MUJOCO_GL: osmesa
      PYOPENGL_PLATFORM: osmesa

    steps:
      - uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          version: "latest"

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install system dependencies for MuJoCo offscreen rendering
        run: |
          sudo apt-get update
          sudo apt-get install -y libosmesa6 libosmesa6-dev libgl1-mesa-glx libglfw3

      - name: Install dependencies
        run: uv sync --group dev

      - name: Lint (flake8)
        run: uv run flake8 src/ tests/

      - name: Type check (mypy)
        run: uv run mypy src/

      - name: Test (pytest)
        run: uv run pytest tests/ -v --cov=src --cov-report=term-missing
